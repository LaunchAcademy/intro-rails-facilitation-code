<h1>Add a New Spell</h1>

<%= form_for([@wizard, @spell]) do |f| %>
  <%= f.label "Name:" %>
  <%= f.text_field :name %>

  <%= f.label "Spell Type:" %>
  <%= f.text_field :spell_type %>

  <%= f.submit %>
<% end %>